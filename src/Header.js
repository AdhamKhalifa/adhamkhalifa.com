import React from 'react';

function Header() {
  return (
    <div id="header" class="section" style={{
      borderRadius: "100%",
      width: "600px",
      height: "600px",
      backgroundColor: "#373372",
      left:"-50px",
      top:"-300px",
      zIndex: "3",
      animation: "fly 12s linear infinite",
      transform:"rotate(0) translate(80px) rotate(0)",
    }}>
        

    </div>
  );
}

export default Header;
